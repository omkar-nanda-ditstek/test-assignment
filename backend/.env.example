# Server Configuration
NODE_ENV=development
PORT=3000

# Cache Configuration
CACHE_TTL=60
CACHE_MAX_SIZE=500

# Logging Configuration
LOG_LEVEL=info

# Rate Limiting (configured in constants, but can be overridden here if needed)
# RATE_LIMIT_WINDOW=60000
# RATE_LIMIT_MAX=10
# BURST_WINDOW=10000
# BURST_CAPACITY=5

# Database Configuration (for mock)
# DATABASE_DELAY=200

# Queue Configuration (BullMQ with Redis)
# ENABLE_QUEUE=false  # Set to 'true' to enable BullMQ queue processing
# REDIS_HOST=localhost
# REDIS_PORT=6379
